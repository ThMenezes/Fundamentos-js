const idadeMinima = 18;
const idadeCliente = 16;

// if(idadeCliente >= idadeMinima){
//    console.log("cerveja");
// } else {
//    console.log("suco");
// }

// Antes do ponto de interrogação é a comparação a ser feita (condição)
// Depois as saídas - primeiro se for verdadeira (true) e depois saída se for (False) Separadas pelos :
console.log(idadeCliente >= idadeMinima ? "cerveja" : "suco");